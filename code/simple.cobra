extern close(fd: int) -> int

type File {
    fd: int,
}

~File(self) {
    close(self.fd);
}

File.read(self, bytes: int) -> string {
    "data"
}

type Box {
    inner: *$a,
}

box(inner: *$a) -> Box<$a> {
    Box{inner}
}

~Box(self) {
    delete self.inner;
}

main() -> int {
    let f = box(new File{1});
    let a = new 5;
    delete a;
    0
}
