struct Point:
    x: int
    y: int

fn add(a: Point, b: Point) -> Point:
    Point{a.x + b.x, a.y + b.y}

fn make_point(x: int, y: int) -> Point:
    Point{x, y}


fn main() -> int:
    let x = make_point(5, 5)
    let y = make_point(7, 7)
    let z = add(x, y)
    z.x + z.y


