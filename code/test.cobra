import io

main() -> int =
    match open("/tmp/foo", WriteOnly):
        Ok{file} => (
            write(file, "Hello World!");
            close(file);
            0
        ),
        Err{err} => (
            println("Failed to open /tmp/foo");
            1
        )
