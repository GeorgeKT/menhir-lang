type Option = Some{int} | None

unwrap_or(opt: Option, default: int) -> int =
    match opt:
        Some{i} => i,
        None => default

main() -> int =
    unwrap_or(Some{5}, 1) + unwrap_or(None, 1)
