type Animal = Dog | Cat | Bird | Fish

number(a: Animal) -> int =
    match a
        Dog => 7,
        Cat => 8,
        Fish => 9,
        _ => 10

main() -> int =
    number(Dog) + number(Fish) + number(Cat) + number(Bird)
