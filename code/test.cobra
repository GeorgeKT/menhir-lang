import "another_file.cobra"

var
    Y = 8888
    A = 18

const X = 7777


struct Foo:
    var x: int = 0, y: int = 0
    pub const z: float = 0.0

    pub func from_string(s: string) -> Foo:
        return Foo(88, 99, 1.0)

    func test(self, a: int):
        x += a

    func __destroy__():
        print("This is the destructor")





func test(a: int) -> int:
    print("A is $a")
    if a > 5:
        return a + 1
    else if a == 5:
        return a
    else:
        return a - 1

    const f = Foo(x, y, z=0.5)
    var i = 0
    while i < 5:
        print("Count: $i")
        i++

    # This is a comment
    for j in 0..10:
        print("foo\n")
