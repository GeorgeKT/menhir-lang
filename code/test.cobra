import "another_file.cobra"

var
    Y = 8888
    A = 18

const X = 7777

trait Clone:
    func clone(self) -> Self


struct Foo:
    pub x: int = 0
    pub y: int = 0
    z: float = 0.0

func bla(b: int) -> int: if b == 6: return b + 1 else: return b - 1 


func test(a: int) -> int:
    print("A is $a")
    if a > 5:
        return a + 1
    else:
        return a - 1

    const f = Foo(x, y, z=0.5)
    var i = 0
    while i < 5:
        print("Count: $i")
        i++

    # This is a comment
    for j in 0..10:
        print("foo\n")
