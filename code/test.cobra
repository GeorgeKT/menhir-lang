pair_or_single(a: int, b: int) -> {int, int} | int =
    if a != b then {a, b} else {a}

value_of(v: {int, int} | int) -> int =
    match v:
        {a, b} => a + b
        {a} => a

main() -> int =
    value_of(pair_or_single(4, 5)) + value_of(pair_or_single(3, 3))
