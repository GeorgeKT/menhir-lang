#ret:58
fn sum(v: int[]) -> int:
    match v:
        [] => 0
        [head | tail] => head + sum(tail)

fn main() -> int:
    let x = [2, 3, 4]
    let sx = sum(x)
    let y = [7; 7]
    let sy = sum(y)
    sx + sy