#ret:15
fn foo(a: int) -> int:
    match a:
        0 => 1
        1 => 2
        _ => 3

fn bar(a: bool) -> int:
    match a:
        true => 4
        false => 5

fn main() -> int:
    foo(1) + bar(true) + bar(false) + foo(0) + foo(10)