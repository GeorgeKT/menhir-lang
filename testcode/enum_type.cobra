#ret:34
enum Animal:
    Dog
    Cat
    Bird
    Fish

fn number(a: Animal) -> int:
    match a:
        Dog => 7
        Cat => 8
        Fish => 9
        _ => 10

fn main() -> int:
    number(Dog) + number(Fish) + number(Cat) + number(Bird)